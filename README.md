# Telegram Supabase Bot

Бот принимает ID записи из таблицы `objects` в Supabase, показывает её содержимое и позволяет редактировать или удалять запись прямо из Telegram.

## Быстрый старт

1. **Подготовить окружение**
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```
2. **Настроить переменные окружения**
   - Скопируйте `.env.example` в `.env`.
   - Заполните значения `TELEGRAM_BOT_TOKEN`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (service role key) и, при необходимости, `SUPABASE_OBJECT_ID_COLUMN`.
   - При блокировках Telegram можно указать `TELEGRAM_PROXY_URL` (например, `socks5://user:pass@host:port`) и задать `TELEGRAM_CONNECT_TIMEOUT` (в секундах).
3. **Запустить бота**
   ```bash
   python bot.py
   ```

## Как работает бот

- `/start` или `/menu` — приветствие, inline‑кнопка и клавиатура с кнопкой «🔍 Найти объект».
- `/object` (синонимы `/o`, `/id`) или кнопка на клавиатуре — бот ожидает ID и подсказывает, что нужно отправить.
- `/app` — присылает кнопку с мини‑приложением, где объекты можно просматривать списком.
- Любой текст без команды воспринимается как ID объекта.
  - Бот загружает запись `objects` по `id` и показывает карточку с ключевыми данными (квартира, дом/ЖК, локация, условия, дополнительные поля). JSON больше не появляется в сообщении.
  - В сообщении появляется Inline‑клавиатура с кнопками **Редактировать** и **Удалить**.
- **Редактирование**
  - После нажатия «Редактировать» бот ждёт JSON с полями для обновления.
  - Пример: `{ "name": "Новое имя", "price": 1200 }`.
  - Бот отправляет PATCH‑запрос в Supabase и показывает обновлённую запись.
- **Удаление**
  - Кнопка «Удалить» запрашивает подтверждение.
  - После подтверждения запись удаляется через DELETE‑запрос.
- При необходимости JSON можно отредактировать вручную через кнопку «Редактировать» — бот ожидает обновлённый JSON в ответ.

## Переменные окружения

| Название | Описание |
| --- | --- |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота от @BotFather. |
| `SUPABASE_URL` | URL вашего проекта Supabase (например, `https://<ref>.supabase.co`). |
| `SUPABASE_SERVICE_ROLE_KEY` | Service role key с правами записи в таблицу `objects`. |
| `SUPABASE_OBJECT_ID_COLUMN` | Название колонки, по которой искать запись (по умолчанию `id`). |
| `TELEGRAM_PROXY_URL` | Необязательный HTTP/SOCKS‑прокси для Telegram API. |
| `TELEGRAM_CONNECT_TIMEOUT` | Необязательный таймаут подключения к Telegram (в секундах). |
| `TELEGRAM_WEBAPP_URL` | Ссылка на развёрнутое мини‑приложение (например, `https://example.com/`). Используется для кнопки `/app`. |
| `WEBAPP_AUTOSTART` | `true/false`. Если `true`, при запуске бота автоматически поднимается локальный `uvicorn miniapp_server`. |
| `WEBAPP_HOST` / `WEBAPP_PORT` | Адрес и порт для локального `uvicorn`, когда включён `WEBAPP_AUTOSTART`. |

## Мини‑приложение (WebApp)

1. **Собрать фронтенд/бекенд**
   - Автозапуск вместе с ботом: выставьте `WEBAPP_AUTOSTART=true` и запустите `python bot.py` — будет поднят встроенный `uvicorn` на `WEBAPP_HOST:WEBAPP_PORT`.
   - Или вручную: `uvicorn miniapp_server:app --reload --host 0.0.0.0 --port 8000`.
   Сервер раздаёт статические файлы из `webapp/` и проксирует запросы в Supabase через `miniapp_server.py`.
2. **Настроить URL**
   - Обновите `TELEGRAM_WEBAPP_URL` в `.env` (например, `https://your-domain.app/` или `http://localhost:8000/` для тестов).
   - Перезапустите бота, чтобы команда `/app` отправляла кнопку с вашим URL.
3. **Возможности мини‑приложения**
   - Оранжевый шапка «HAPPINESS CRM», список объектов по умолчанию.
   - Поиск по ID/адресу в реальном времени.
   - В каждой карточке: ID, адрес, цена, базовые характеристики. Кнопка «Добавить объект» зарезервирована (в разработке).
   - Приложение готово к подключению как Telegram Web App, но может работать и как обычная SPA в браузере.

## Проверка

- Используйте любой ID из таблицы `objects`.
- Проверьте сценарии: успешный просмотр, редактирование (валидный JSON), ошибка JSON, удаление и отмена удаления.

## Безопасность

- Храните ключи только в `.env` (не коммитьте файл).
- Service role key выдаёт полный доступ, поэтому не делитесь им публично и ограничьте доступ к серверу, где запускается бот.
